
export function iterate2d(array, h, w) {
  // TODO: figure out how to chunk a 2d array into 2d pieces by height and width

  const iterations = [];

  for (let y = 0; y <= array.length - h; y++) {
    const iteration = [];
  }

  return iterations;
}
